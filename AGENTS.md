# AGENTS.md

dsh-operation-improve 是Deepseek harness的使用体验增强插件, 改动前先读 [README.md](README.md)
- 约束的理由与踩坑排查指南见 [rationale.md](rationale.md).
- 任务交接索引见 [handoff.md](handoff.md)，执行期间的维护义务见「Handoff 交接」一节。

## Handoff 交接

- **你必须维护 handoff。** 执行任务期间，[handoff.md](handoff.md) 是唯一交接索引：一张表记录每条交接的编号、标题、状态与完整记录链接；完整记录一个文件一条，放 [docs/handoff/](docs/handoff/)。
- 每次 Agent 执行一个具有实际代码变更的任务时，必须维护 Handoff 状态。
- 状态只有三个值：未开始、正在做、完成。「正在做」全表至多一条，该行即当前交接。
- 任务开始先登记索引行；推进到可验证的节点就更新完整记录；完成时状态改「完成」并补齐验证方式；中断改回「未开始」并在记录里写明原因。
- 索引只放一行摘要，过程细节、决策理由、遗留问题只写进完整记录——一处事实一个家。
- 具体维护流程按技能 `handoff-maintenance` 执行；接手任务时先读索引定位当前交接，再读对应完整记录续接。

注意: 必须经由我确认完成后索引行状态才能改为 `完成`

### 写当前状态，不写变更史

durable 散文里不出现「之前 / 现在 / 不再 / 原来是 / 已改名」，也不出现 PR 号和 commit hash——直接写现在的机制是什么，变更故事留给 commit message。例外只有 [handoff.md](handoff.md) 的索引表、[docs/handoff/](docs/handoff/) 的交接记录和显式标了**临时**的小节：它们记录的就是状态迁移本身，回退时连小节一起删。

### 散文规则

- **一个自然段写成一个物理行**，靠编辑器软换行折行。代码块、表格、列表结构保持原样。
- **仓内引用一律用相对路径的 Markdown 链接**，不写裸文件名。「见 README」不算引用。
- **注释只写代码表达不了的契约或理由**，不复述代码。契约指前置条件、后置条件、不变量、失败模式、所有权、时序、兼容承诺。
- **写 `contract`/`boundary`/`shape`/`surface` 之前先找更准确的词**：能说「响应字段」「JSON 校验」「ESM 导出」就别说 "response shape"、"validation boundary"、"module shape"。真的是进程/安全边界、真的是调用方依赖的契约时才保留。
- **加粗留给会改变行为的那一句**。到处加粗等于没加粗。

改完自查，命中就改：

- 同一条规则出现在两个地方。grep 一个特征短语，留一个家，其余改成链接。
- 叙述历史：「之前」「现在」「不再」「已迁移」「改名为」。
- 实现状态注解混进散文或图里（「已实现！」「future: …」）。状态会烂，仓库布局和 manifest 自己会说。
- 手抄的清单：包列表、依赖列表、插件表——源码或 manifest 才是权威。
- 推理过程：一步步的实现叙述、显然分支的证明、被否掉的本地方案。留结论契约，删推导路径。
- 段落墙：一段塞好几条规则加括号补充。拆开，或者把细节降到它自己的家里。
